1er jet:

Plutot simple, il suffit d'utiliser gdb sur le binaire, puis désassembler le main

On identifie facilement une comparaison juste après le scanf
Il compare eax à une valeur en hexa (0x149c = 5276 en décimal)

Et voilà le mdp pour passer le check qui va ensuite lancer system("/bin/sh")
